<div class="row">


      <% (0...@album.images.count).each do |image| %>
       <div class="card col-md-4 mb-3 p-2 " >

     <%= image_tag(@album.thumbnail(image)) %>

      <div class="card-body">

      </div>
    </div>
    <% end %>
  </div>
